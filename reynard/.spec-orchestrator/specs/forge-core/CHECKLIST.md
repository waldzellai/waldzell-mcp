# Checklist: forge-core
- [ ] Manifest read/write validates with zod and fails on invalid input.
- [ ] Hashing deterministic across key order and runs.
- [ ] UUID helper used across packages.
- [ ] scoreNovelty computes BD/SD/SP with archive lookup and thresholds.
- [ ] Tests cover manifest validation errors and novelty pass/fail cases.
