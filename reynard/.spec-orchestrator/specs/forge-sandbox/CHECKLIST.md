# Checklist: forge-sandbox
- [ ] docker run enforces CPU/mem/time; processes killed on exceed.
- [ ] RO rootfs with configured writable mounts.
- [ ] Network allowlist enforced; testEgress reports allow/deny.
- [ ] Outputs include exitCode, stdout, stderr, duration.
- [ ] Errors helpful when Docker unavailable.
