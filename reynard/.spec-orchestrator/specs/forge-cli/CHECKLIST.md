# Checklist: forge-cli
- [ ] Each command invokes underlying package (no TODO prints).
- [ ] Config validated; defaults shown; clear errors with exit code >0.
- [ ] propose/eval/select stream progress and return candidate IDs/winners.
- [ ] reproduce uses manifest to rerun deterministically.
- [ ] Smoke run on demo succeeds end-to-end.
