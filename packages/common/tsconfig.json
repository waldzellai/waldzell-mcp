{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    // Add any common-package specific compiler option overrides here if needed
    // For example, if it generates different module types or needs specific libs
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
