import type { GraphSpec } from '../../forge-core/src/types.js';

/** TODO: generate TS code per node + tests, write to /archives/agents/<id> */
export async function realizeGraph(g: GraphSpec, outDir: string) {
  // TODO: scaffold src/, tests/, manifest.json
  return { outDir };
}