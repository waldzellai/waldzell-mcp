{
  "name": "simple-parser",
  "version": "1.0.0",
  "io": {
    "inputSchema": {},
    "outputSchema": {}
  },
  "invariants": [
    "must not return null tokens",
    "length(output.tokens) > 0 for non-empty input"
  ]
}